{\Large\centerline{\begin{varwidth}{\textwidth}
  \def\next{\medskip\\}
  \displaykey{Compose + O + R} = ® \next
  \displaykey{Compose + 3 + 4} = ¾ \next
  \displaykey{Compose + s + s} = ß \next
  \displaykey{Compose + \textasciitilde\ + ' + a} = ấ
\end{varwidth}}}
